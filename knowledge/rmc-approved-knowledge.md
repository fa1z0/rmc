# Legacy prototype knowledge file

The Knowledge Hub version now uses OpenAI File Search and an RMC vector store.

Upload approved RMC source documents through `/admin.html`.

This Markdown file is retained only as a reference from the earlier prototype and is not used by `server.js`.
